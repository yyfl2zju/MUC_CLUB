<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.club.management.mapper.MemberMapper">

    <resultMap id="MemberWithDeptName" type="com.club.management.entity.Member">
        <id property="id" column="id"/>
        <result property="stuId" column="stu_id"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="college" column="college"/>
        <result property="major" column="major"/>
        <result property="grade" column="grade"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="joinDate" column="join_date"/>
        <result property="deptId" column="dept_id"/>
        <result property="deptName" column="deptName"/>
        <result property="role" column="role"/>
        <result property="password" column="password"/>
        <result property="leaveDate" column="leave_date"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectMemberPage" resultMap="MemberWithDeptName">
        SELECT m.id, m.stu_id, m.name, m.gender, m.college, m.major, m.grade, 
               m.phone, m.email, m.join_date, m.leave_date, m.dept_id, m.role, 
               m.password, m.create_by, m.create_time, m.update_time, d.name as deptName
        FROM member m
        LEFT JOIN dept d ON m.dept_id = d.id
        <where>
            <if test="name != null and name != ''">
                AND m.name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="stuId != null and stuId != ''">
                AND m.stu_id LIKE CONCAT('%', #{stuId}, '%')
            </if>
            <if test="deptId != null">
                AND m.dept_id = #{deptId}
            </if>
            <if test="role != null and role != ''">
                AND m.role = #{role}
            </if>
        </where>
        <choose>
            <when test="sortField != null and sortField != ''">
                ORDER BY 
                <if test="sortField == 'stu_id'">m.stu_id</if>
                <if test="sortField == 'name'">m.name</if>
                <if test="sortField == 'join_date'">m.join_date</if>
                <if test="sortOrder == 'asc'">ASC</if>
                <if test="sortOrder == 'desc'">DESC</if>
            </when>
            <otherwise>
                ORDER BY m.create_time DESC
            </otherwise>
        </choose>
    </select>

    <!-- 获取社员参与的活动 -->
    <select id="selectMemberActivities" resultType="java.util.Map">
        SELECT 
            a.id,
            a.name,
            a.type,
            a.start_time as startTime,
            a.end_time as endTime,
            a.status,
            a.create_time as createTime,
            am.signup_time as signupTime,
            am.signup_status as signupStatus
        FROM activity_member am
        LEFT JOIN activity a ON am.activity_id = a.id
        WHERE am.member_id = #{memberId}
        ORDER BY a.start_time DESC
    </select>

    <!-- 搜索社员信息（包含部门名称） -->
    <select id="searchMembersWithDept" resultType="java.util.Map">
        SELECT 
            m.id,
            m.stu_id as stuId,
            m.name,
            m.gender,
            m.college,
            m.major,
            m.grade,
            m.phone,
            m.email,
            m.role,
            m.dept_id as deptId,
            d.name as deptName
        FROM member m
        LEFT JOIN dept d ON m.dept_id = d.id
        <where>
            (m.name LIKE CONCAT('%', #{q}, '%') OR m.stu_id LIKE CONCAT('%', #{q}, '%'))
            <if test="role != null and role != ''">
                AND m.role = #{role}
            </if>
        </where>
        ORDER BY m.create_time DESC
    </select>

</mapper>
