<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.club.management.mapper.SysUserMapper">

    <!-- 搜索系统用户信息（包含部门名称） -->
    <select id="searchSysUsersWithDept" resultType="java.util.Map">
        SELECT 
            su.id,
            su.stu_id as stuId,
            su.name,
            su.role,
            su.dept_id as deptId,
            d.name as deptName
        FROM sys_user su
        LEFT JOIN dept d ON su.dept_id = d.id
        <where>
            (su.name LIKE CONCAT('%', #{q}, '%') OR su.stu_id LIKE CONCAT('%', #{q}, '%'))
            <if test="role != null and role != ''">
                AND su.role = #{role}
            </if>
        </where>
        ORDER BY su.create_time DESC
    </select>

</mapper>
